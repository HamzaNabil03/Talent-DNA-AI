# ADR 0002: React, Laravel, and MySQL

Status: Accepted

Use React 19 + TypeScript + Vite for the Arabic-first SPA, Laravel 12 for the REST API, and MySQL 8 as the system of record. OpenAPI under `packages/contracts` generates the frontend contract and prevents silent drift.
