[build]
builder = "DOCKERFILE"
dockerfilePath = "infra/docker/Dockerfile"

[deploy]
startCommand = "start-web"
healthcheckPath = "/api/v1/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
